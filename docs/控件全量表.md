# EasyUCD 控件全量表

> 由 `scripts/gen-controls.mjs` 从 `scripts/control-catalog.mjs` 自动生成，请勿手改。共 90 个控件。

## 容器（7）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Page | 400×600 | 整页容器：全框 + 左上标题 | `<body>` 顶层主区 或 `<main>` |
| Section | 360×200 | 区块容器：全框 + 左上标题 | `<section>` |
| Card | 240×160 | 卡片：顶部图片条 + 标题 + 正文行 | `<div class="card">` |
| Modal | 320×240 | 模态框：标题栏 + 关闭 × + 底部按钮 | `<dialog open>` 或 `<div role="dialog">` |
| Drawer | 280×600 | 侧边抽屉：标题栏 + 关闭 × + 内容行 | `<aside>` 或侧向 `<dialog>` |
| Collapse | 320×120 | 折叠面板：多条标题行 + 右侧 ▸ 角标 | `<details><summary>…</summary></details>` |
| Splitter | 360×200 | 分栏：中部竖直分隔线 + 手柄 | `display:flex` + 拖拽分隔条 |

## 导航（12）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Header | 400×60 | 页头：左 logo + 右侧导航文字 | `<header>` |
| Footer | 400×60 | 页脚：居中版权/分栏文字 | `<footer>` |
| Nav | 400×44 | 导航条：一排导航项 | `<nav>` |
| Tabs | 320×40 | 标签页：底部基线 + 首个下划线高亮 | `<div role="tablist">` + 若干 `<button role="tab">` |
| Breadcrumb | 300×24 | 面包屑：纯文字路径（无边框） | `<nav aria-label="breadcrumb"><ol><li>…` |
| Menu | 200×240 | 菜单：多条「icon + 文字」 + 分隔线 | `<nav>` + `<ul>` 菜单 |
| Sidebar | 220×600 | 侧栏：顶部 logo + 多条菜单项 | `<aside>` 侧栏导航 |
| Toolbar | 360×44 | 工具栏：一排小按钮 + 竖分隔 | `<div role="toolbar">` |
| Pagination | 240×32 | 分页：一排小方格（首格高亮） | `<nav aria-label="pagination"><ul><li>…` |
| Steps | 360×48 | 步骤条：圆点 + 连接线 + 步骤文字 | `<ol>` 步骤列表 |
| Anchor | 160×120 | 锚点：左竖线 + 锚点圆点 + 文字 | `<nav>` 锚点目录 |
| Dropdown | 160×40 | 下拉菜单触发：文字 + 右 ▾ | `<details>` / menu button |

## 输入（8）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Input | 240×40 | 文本框：全框 + 左侧占位文字 | `<input type="text">`（text 作 placeholder 或前置 `<label>`） |
| Password | 240×40 | 密码框：占位文字 + 右侧眼睛 | `<input type="password">` |
| IPInput | 240×40 | IP 输入框：四段八位 + 点分隔 | IP 地址输入：四段 `<input inputmode="numeric" maxlength="3">` + `.` 分隔（JS 自动跳段、校验 0–255） |
| Textarea | 240×100 | 多行框：左上占位 + 右下缩放纹 | `<textarea>` |
| Select | 240×40 | 下拉选择：文字 + 右 ▾ | `<select>` + `<option>` |
| Checkbox | 160×24 | 复选：勾选方块 + 文字 | `<label><input type="checkbox">…</label>` |
| Radio | 160×24 | 单选：圆 + 内圆点 + 文字 | `<label><input type="radio">…</label>` |
| Switch | 56×28 | 开关：胶囊轨道 + 右侧圆钮 | `<label><input type="checkbox" role="switch">…</label>` |

## 表单进阶（14）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Slider | 240×24 | 滑块：水平轨道线 + 中部圆钮 | `<input type="range">` |
| NumberInput | 120×40 | 数字步进：数字 + 右上下 ▲▼ | `<input type="number">` |
| DatePicker | 200×40 | 日期选择：日期占位 + 右日历图标 | `<input type="date">` |
| TimePicker | 160×40 | 时间选择：时间占位 + 右时钟图标 | `<input type="time">` |
| DateRange | 280×40 | 日期范围：起止占位 + 右日历图标 | 两个 `<input type="date">` |
| Upload | 240×176 | 文件上传：虚线拖拽区 + ↑ + 提示 + 已选文件列表（含删除） | `<input type="file">` 拖拽上传区 + 已选文件列表（文件名 + 删除 ×；多选/进度条可选，JS 渐进增强） |
| Rate | 160×28 | 评分：5 颗星（前 3 实心） | 星级 `role="radiogroup"`（若干 `<input type="radio">`） |
| ColorPicker | 160×40 | 取色：左色块 + 色值文字 | `<input type="color">` |
| SearchBox | 240×40 | 搜索框：左放大镜 + 占位文字 | `<input type="search">` |
| Cascader | 240×40 | 级联选择：多级路径 + 右 ▾ | 级联 `<select>`（多级联动） |
| AutoComplete | 240×40 | 自动完成：输入 + 下拉候选条 | `<input list="...">` + `<datalist>` |
| TagInput | 240×40 | 标签输入：内含标签胶囊 + 光标 | 标签 + `<input>`（可输入多标签） |
| Form | 360×320 | 表单：标题 + 多组「标签+输入」 + 提交 | `<form>`（包裹其内表单控件） |
| FormItem | 320×56 | 表单项：左标签 + 右输入框 | `<div class="form-item">` label + control |

## 选择进阶（9）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| TreeSelect | 240×40 | 树选择：▸ + 已选文字 + 右 ▾ | 树形 combobox（`<select>` 衍生，可勾选树） |
| TreeTable | 320×180 | 树形表格：表头 + 行线 + 首列层级缩进 ▸ | `<table>` + 可展开行（树形表格） |
| MultiSelect | 240×40 | 多选下拉：内含标签胶囊 + 右 ▾ | `<select multiple>` / 带 tag 多选 |
| CheckboxGroup | 240×96 | 复选组：多行「方块 + 文字」 | 一组 `<label><input type="checkbox">` |
| RadioGroup | 240×96 | 单选组：多行「圆 + 内点 + 文字」 | 一组 `<label><input type="radio">` |
| Transfer | 320×180 | 穿梭框：左右双列表 + 中间 ‹ › 移动 | 双 `<ul>` + 移动按钮（穿梭框） |
| Segmented | 240×36 | 分段器：竖分隔分段（首段高亮） | 分段 `role="radiogroup"`（segmented） |
| Mentions | 240×80 | 提及输入：@name + 候选下拉条 | `<textarea>` + @ 提及候选 |
| CheckableTag | 72×26 | 可选标签：圆角胶囊（选中态） | 可勾选 `<span class="tag">`（`<label>` 包裹） |

## 展示（8）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Heading | 240×36 | 标题：大字号纯文字 | `<h1>`～`<h3>`（按层级/尺寸择一） |
| Text | 240×24 | 正文：纯文字 | `<p>` |
| Image | 200×140 | 图片占位：全框 + 对角交叉线 | `<img alt="…">`（占位 `https://placehold.co/WxH`） |
| Icon | 32×32 | 图标：方框 + 居中符号 | `<span aria-hidden="true">`（图标占位） |
| Avatar | 48×48 | 头像：圆形 + 字母/人形 | 圆形 `<img alt="…">` 或占位 `<div>` |
| Badge | 44×22 | 徽标：小胶囊 + 数字/文字 | `<span class="badge">` |
| Tag | 72×26 | 标签：圆角胶囊 + 左圆点 + 文字 | `<span class="tag">` |
| Divider | 320×16 | 分割线：水平线 | `<hr>` |

## 反馈与状态（11）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Alert | 320×48 | 警告条：左色条 + 图标 + 文字 + × | `<div role="alert">` |
| Toast | 280×64 | 通知：图标 + 标题 + 描述 + × | `<div role="status">`（通知/吐司） |
| Tooltip | 120×32 | 提示气泡：小框 + 文字 + 底部三角 | `<div role="tooltip">` |
| Popover | 200×120 | 气泡卡：标题 + 正文 + 指示三角 | `<div role="dialog">`（popover） |
| Popconfirm | 220×100 | 确认气泡：提示 + 底部两按钮 | 确认气泡（popover + 确认/取消按钮） |
| Progress | 240×12 | 进度条：轨道矩形 + 60% 填充 | `<progress>` 或 `<div role="progressbar">` |
| ProgressCircle | 64×64 | 环形进度：圆环 + 中央百分比 | 环形 `<div role="progressbar">` |
| Spinner | 40×40 | 加载圈：圆环 + 缺口指示 | 加载指示器 `<div role="status">` |
| Skeleton | 240×80 | 骨架屏：多条灰条（宽度递减） | 骨架占位 `<div class="skeleton">` |
| Empty | 200×140 | 空状态：占位图 + 「暂无数据」 | 空状态 `<div>`（插画 + 文案） |
| Result | 240×160 | 结果页：大圆图标 + 标题 + 描述 | 结果页 `<div>`（状态图 + 标题 + 操作） |

## 数据展示（14）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| List | 240×160 | 列表：多条「圆点 + 文字 + 分隔线」 | `<ul>` / `<ol>` + `<li>` |
| Table | 320×160 | 表格：表头分隔 + 行线 + 列线 | `<table>` + `<thead>` / `<tbody>` |
| PagedTable | 320×208 | 分页表格：表头 + 行线 + 列线 + 底部分页条 | `<table>`（`<thead>`/`<tbody>`）+ 底部 `<nav aria-label="pagination">` 分页器；JS 翻页（渐进增强，下载后生效） |
| Grid | 320×200 | 网格：2×3 网格分隔线 | `display:grid` 的容器 `<div>` + 网格项 |
| Tree | 240×180 | 树：多层级行（缩进 + ▾/▸） | 树形 `<ul>`（嵌套缩进） |
| CheckTree | 240×180 | 可勾选树：多层级 + ▾/▸ 折叠 + 复选方块（父子级联勾选） | 树形 `<ul>` + 每节点 `<input type="checkbox">`；▾/▸ 折叠；勾父节点级联勾/取消全部子节点、子节点部分选中时父节点 `indeterminate` 半选（JS 渐进增强） |
| Timeline | 240×200 | 时间线：左竖线 + 节点圆点 + 文字 | `<ol>` 时间线（节点 + 内容） |
| Statistic | 160×80 | 统计数值：标题 + 大数字 | 统计卡 `<div>`（标题 + 数值） |
| Descriptions | 320×160 | 描述列表：多行「标签：值」 | `<dl>` + `<dt>` / `<dd>` |
| Calendar | 280×240 | 日历：月份栏 + 7×5 日期网格 | 日历网格 `<table>` / `<div>` |
| Carousel | 320×180 | 轮播：图片占位 + 左右 ‹ › + 指示点 | 轮播 `<div>`（图片 + 指示点） |
| BarChart | 240×160 | 柱状图：基线 + 高低不一的柱 | 图表占位 `<div>`（柱状） |
| LineChart | 240×160 | 折线图：基线 + 多段折线 | 图表占位 `<div>`（折线） |
| PieChart | 160×160 | 饼图：外圆 + 半径分割线 | 图表占位 `<div>`（饼图） |

## 动作（4）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Button | 120×40 | 按钮：实心观感框 + 居中文字 | `<button>` |
| Link | 100×24 | 链接：带下划线文字 | `<a href="#">` |
| ButtonGroup | 240×40 | 按钮组：竖分隔分 3 段 | 一组 `<button>`（相邻分段） |
| FAB | 56×56 | 悬浮按钮：圆形 + 中央 ＋ | 圆形浮动 `<button>`（FloatButton） |

## 媒体（3）

| type | 默认 w×h | 设计要素 | HTML 映射 |
|------|---------|---------|-----------|
| Video | 320×180 | 视频：播放圆钮 + 底部控制条 | `<video controls>` |
| Audio | 280×40 | 音频：播放钮 + 进度线 + 时间 | `<audio controls>` |
| Map | 320×200 | 地图：街道网格 + 中央标记点 | 地图占位 `<div>`（街道 + 标记） |
